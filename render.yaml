services:
  - type: web
    name: ml-service
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT ml_service:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
